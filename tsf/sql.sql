CREATE TABLE `tsf`.`pena` (
  `id` INT NOT NULL,
  `cif` VARCHAR(45),
  `nombre` VARCHAR(45) NOT NULL,
  `email` VARCHAR(45),
  `web` VARCHAR(45),
  `telefono` VARCHAR(45),
  `sede` VARCHAR(45),
  `numCuenta` VARCHAR(45),
  PRIMARY KEY (`id`));

